# 项目目标：易用性第一位的学生用的日常辅助数学思考的符号计算系统。（sympy gui版）

# 借助QWebEngineView写一个浏览器，把聚合搜索网站作为主页。
# 待办：
# github官网挂上win11exe下载的tag/realease（并写在README里面）（现状：不加--nonconsole就能正常运行）
# 性能优化以及多进程优化。
# 重新回顾这个项目，把学到的东西写入笔记并整理。
# 创建多项式处理功能页面。
# 做一些输入时的错误检查。
# QSS文件外置，来美化。
# 支持自定义壁纸
# 完善思维导图。
# 编写相关文档。
# docs：api，tutorial的编写
# 折叠起的菜单栏：自定义图片背景功能，通过文件读写来记忆背景图片，github官网，settings
# 配置文件，设置开启启动的界面：homepage（可选：是否显示），calculus，linear_algebra，...，上次关闭时的页面。
# 设计服务器云计算 checkbox。（服务器那边做好相关防护）（鼠标移到checkbox上时显示消息：若服务器负载过大可能会响应慢或无响应）
# 我上传到 github，服务器定时自动从github更新文件，客户端联网更新从服务器获取新版本。（可取消更新弹窗）

#另：记得版本更新后，重新打包exe，上传到服务器。